<?xml version="1.0" encoding="utf-8"?>
<CodeBlockFile xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" Id="08d83a35-38db-45c5-a3de-f1d48616bfa9" Language="Lua" Name="SettingsLogic" Type="Logic" Source="None" ModifyTime="2026-08-03T10:05:16.2149954+09:00">
  <CoreVersion Major="0" Minor="2" />
  <ScriptVersion Major="1" Minor="1" />
  <Description />
  <Properties>
    <CodeBlockProperty Name="settingsGroup" Type="Entity" DefaultValue="13222179-5ab0-45c7-b2f5-699c5541ec00" SyncDirection="None" />
    <CodeBlockProperty Name="openBtn" Type="Component" DefaultValue="d3d2d59e-c951-4d8d-ae1d-25569b073373:ButtonComponent" SyncDirection="None" />
    <CodeBlockProperty Name="hideWhileOpen" Type="Entity" DefaultValue="b12cf809-0d29-49a7-9e05-51f6829610dd" SyncDirection="None" />
    <CodeBlockProperty Name="sliderBgm" Type="Component" DefaultValue="1e61997b-e20c-4711-9be0-6bad3196a567:SliderComponent" SyncDirection="None" />
    <CodeBlockProperty Name="valueBgm" Type="Component" DefaultValue="3aaf2490-7641-452a-829d-433fa68c046c:TextGUIRendererComponent" SyncDirection="None" />
    <CodeBlockProperty Name="sliderBgmTouch" Type="Component" DefaultValue="1e61997b-e20c-4711-9be0-6bad3196a567:UITouchReceiveComponent" SyncDirection="None" />
    <CodeBlockProperty Name="sliderSfx" Type="Component" DefaultValue="b05d38b0-319a-4ef5-8cd6-5d3b29f4bde9:SliderComponent" SyncDirection="None" />
    <CodeBlockProperty Name="valueSfx" Type="Component" DefaultValue="b82ff34a-c646-4f6c-b781-14f86feed0d9:TextGUIRendererComponent" SyncDirection="None" />
    <CodeBlockProperty Name="sliderSfxTouch" Type="Component" DefaultValue="b05d38b0-319a-4ef5-8cd6-5d3b29f4bde9:UITouchReceiveComponent" SyncDirection="None" />
    <CodeBlockProperty Name="keyText1" Type="Component" DefaultValue="5f531321-da9d-43fd-b1e8-b5a28513ef2d:TextGUIRendererComponent" SyncDirection="None" />
    <CodeBlockProperty Name="btnChange1" Type="Component" DefaultValue="0cfabdac-69d7-4e07-9458-fda3a94bd4a8:ButtonComponent" SyncDirection="None" />
    <CodeBlockProperty Name="waitIcon1" Type="Entity" DefaultValue="9bcee183-0af4-4b41-a550-bb23de5f7783" SyncDirection="None" />
    <CodeBlockProperty Name="keyText2" Type="Component" DefaultValue="a5092338-5ef3-4e29-8940-55ab92a62ac0:TextGUIRendererComponent" SyncDirection="None" />
    <CodeBlockProperty Name="btnChange2" Type="Component" DefaultValue="e711282c-7395-4ee8-b33c-8032100dd133:ButtonComponent" SyncDirection="None" />
    <CodeBlockProperty Name="waitIcon2" Type="Entity" DefaultValue="e9652562-be58-4862-a607-6263785234b6" SyncDirection="None" />
    <CodeBlockProperty Name="keyText3" Type="Component" DefaultValue="239d89e9-ee0c-4b43-81dd-f8d19e9f693b:TextGUIRendererComponent" SyncDirection="None" />
    <CodeBlockProperty Name="btnChange3" Type="Component" DefaultValue="c260e9c0-4770-4200-b841-46a8650d3b54:ButtonComponent" SyncDirection="None" />
    <CodeBlockProperty Name="waitIcon3" Type="Entity" DefaultValue="780742b2-992c-431f-8c18-bade514a7551" SyncDirection="None" />
    <CodeBlockProperty Name="btnRevertKeys" Type="Component" DefaultValue="5cff0dfe-173b-4834-8c69-5103c4a30fe7:ButtonComponent" SyncDirection="None" />
    <CodeBlockProperty Name="syncValueText" Type="Component" DefaultValue="999e76f6-a58c-407c-a910-0158ee54c32b:TextGUIRendererComponent" SyncDirection="None" />
    <CodeBlockProperty Name="btnSyncAdjust" Type="Component" DefaultValue="76fe576f-a671-4876-9d68-2d47e7d8c411:ButtonComponent" SyncDirection="None" />
    <CodeBlockProperty Name="btnApply" Type="Component" DefaultValue="56cad499-b1ae-4f10-8fc9-478836ca6b92:ButtonComponent" SyncDirection="None" />
    <CodeBlockProperty Name="btnClose" Type="Component" DefaultValue="5d24552a-031b-4ab3-90da-133cd9db8a9f:ButtonComponent" SyncDirection="None" />
    <CodeBlockProperty Name="noticeLabel" Type="Entity" DefaultValue="nil -- (선택) 안내 문구 엔티티" SyncDirection="None" />
    <CodeBlockProperty Name="noticeText" Type="Component" DefaultValue="nil -- (선택) 안내 문구 텍스트:TextGUIRendererComponent" SyncDirection="None" />
    <CodeBlockProperty Name="action1" Type="string" DefaultValue="&quot;MoveLeft&quot;" SyncDirection="None" />
    <CodeBlockProperty Name="action2" Type="string" DefaultValue="&quot;MoveRight&quot;" SyncDirection="None" />
    <CodeBlockProperty Name="action3" Type="string" DefaultValue="&quot;Jump&quot;" SyncDirection="None" />
    <CodeBlockProperty Name="waitingLabel" Type="string" DefaultValue="&quot;입력 대기&quot;" SyncDirection="None" />
    <CodeBlockProperty Name="noticeDurationSeconds" Type="number" DefaultValue="1.6" SyncDirection="None" />
    <CodeBlockProperty Name="localSyncOffsetMs" Type="integer" DefaultValue="0" SyncDirection="None" />
    <CodeBlockProperty Name="syncStepMs" Type="integer" DefaultValue="10" SyncDirection="None" />
    <CodeBlockProperty Name="syncMinMs" Type="integer" DefaultValue="-100" SyncDirection="None" />
    <CodeBlockProperty Name="syncMaxMs" Type="integer" DefaultValue="100" SyncDirection="None" />
    <CodeBlockProperty Name="isOpen" Type="boolean" DefaultValue="false" SyncDirection="None" />
    <CodeBlockProperty Name="waitingAction" Type="string" DefaultValue="&quot;&quot;" SyncDirection="None" />
    <CodeBlockProperty Name="noticeTimerId" Type="integer" DefaultValue="0" SyncDirection="None" />
    <CodeBlockProperty Name="sliderBgmHandler" Type="any" DefaultValue="nil" SyncDirection="None" />
    <CodeBlockProperty Name="sliderSfxHandler" Type="any" DefaultValue="nil" SyncDirection="None" />
    <CodeBlockProperty Name="sliderSfxTouchHandler" Type="any" DefaultValue="nil" SyncDirection="None" />
    <CodeBlockProperty Name="btnRevertKeysHandler" Type="any" DefaultValue="nil" SyncDirection="None" />
    <CodeBlockProperty Name="btnSyncAdjustHandler" Type="any" DefaultValue="nil" SyncDirection="None" />
    <CodeBlockProperty Name="btnApplyHandler" Type="any" DefaultValue="nil" SyncDirection="None" />
    <CodeBlockProperty Name="btnCloseHandler" Type="any" DefaultValue="nil" SyncDirection="None" />
    <CodeBlockProperty Name="openBtnHandler" Type="any" DefaultValue="nil" SyncDirection="None" />
    <CodeBlockProperty Name="keyDownHandler" Type="any" DefaultValue="nil" SyncDirection="None" />
    <CodeBlockProperty Name="settingsHandler" Type="any" DefaultValue="nil" SyncDirection="None" />
  </Properties>
  <Methods>
    <CodeBlockMethod Name="OnBeginPlay" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments />
      <Code><![CDATA[
-- ==============================================================
-- [변경] 런타임 상태와 동작 설정은 프로퍼티가 아니라 self._T에 둔다.
-- 에디터에 새 프로퍼티를 추가하면 기존 프로젝트에 등록되지 않아
-- "cannot set X, no such field"로 죽는 일이 있어서, 선언이 필요 없는
-- _T를 쓴다. 아래 두 값이 이 화면의 동작 스위치다.
-- ==============================================================
-- true  = action1~3 프로퍼티에 뭐가 들어 있든 리듬 액션으로 고정
-- false = 프로퍼티 패널의 값을 그대로 사용
self._T.forceRhythmActions = true

-- true  = 키를 누른 뒤 [변경]을 눌러야 저장 (변경 버튼이 저장 시점)
-- false = [변경] 누르고 키를 누르면 그 자리에서 저장
self._T.confirmWithButton = true

-- 입력만 받아 두고 아직 저장하지 않은 키("" = 없음)
self._T.pendingKeyName = ""

-- 슬롯을 아직 다 연결하지 않아도 멈추지 않는다. 비어 있는 슬롯은 건너뛰고
-- 무엇이 빠졌는지 로그로 알려주므로 UI를 만들어가며 단계적으로 확인할 수 있다.
if isvalid(self.settingsGroup) then
	self.settingsGroup.Enable = false
else
	log("[SettingsLogic] settingsGroup 미연결 — 패널을 열 수 없다")
end

-- [추가] 이 화면은 리듬 커맨드 키 화면이므로 세 행을 강제로 고정한다.
-- action1~3 프로퍼티 값은 프로젝트 파일에 저장되어 스크립트의 기본값을
-- 덮어쓴다. 거기에 MoveLeft/MoveRight/Jump가 들어가 있으면 화면이
-- 캐릭터 조작 키(A/D/Space)를 만지게 되고, 전투 키는 그대로 남는다.
-- 프로퍼티 패널을 손대지 않아도 맞게 동작하도록 여기서 바로잡는다.
-- 다른 액션을 붙이고 싶으면 forceRhythmActions를 false로 끄면 된다.
if self._T.forceRhythmActions then
	if self.action1 ~= "BeatLeft" or self.action2 ~= "BeatRight" or self.action3 ~= "BeatConfirm" then
		log("[SettingsLogic] 키 행이 리듬 액션이 아니어서 바로잡음: "
			.. tostring(self.action1) .. "/" .. tostring(self.action2) .. "/" .. tostring(self.action3)
			.. " → BeatLeft/BeatRight/BeatConfirm")
	end
	self.action1 = "BeatLeft"
	self.action2 = "BeatRight"
	self.action3 = "BeatConfirm"
end

self._T.slots = {
	{ action = self.action1, btn = self.btnChange1, text = self.keyText1, icon = self.waitIcon1 },
	{ action = self.action2, btn = self.btnChange2, text = self.keyText2, icon = self.waitIcon2 },
	{ action = self.action3, btn = self.btnChange3, text = self.keyText3, icon = self.waitIcon3 },
}

self:BuildDisplayNames()

log("[SettingsLogic] 키 행 연결: 1번=" .. tostring(self.action1)
	.. " / 2번=" .. tostring(self.action2)
	.. " / 확인=" .. tostring(self.action3))

-- 열기 버튼을 가장 먼저 연결한다. 다른 슬롯이 비어 있어도 창은 열리게.
if self.openBtn ~= nil then
	self.openBtnHandler = self.openBtn.Entity:ConnectEvent(ButtonClickEvent, self.OnOpenBtnClick)
else
	log("[SettingsLogic] openBtn 미연결 — _SettingsLogic:Toggle()로 열어야 한다")
end

for i, slot in ipairs(self._T.slots) do
	if isvalid(slot.icon) then slot.icon.Enable = false end
	if slot.btn ~= nil then
		slot.clickHandler = slot.btn.Entity:ConnectEvent(ButtonClickEvent, function()
			self:OnKeyButtonClick(slot)
		end)
	else
		log("[SettingsLogic] btnChange" .. tostring(i) .. " 미연결 — 그 행의 키 변경 비활성")
	end
end

if self.sliderBgm ~= nil then
	self.sliderBgmHandler = self.sliderBgm.Entity:ConnectEvent(SliderValueChangedEvent, self.OnBgmChanged)
end
if self.sliderSfx ~= nil then
	self.sliderSfxHandler = self.sliderSfx.Entity:ConnectEvent(SliderValueChangedEvent, self.OnSfxChanged)
end
if self.sliderSfxTouch ~= nil then
	self.sliderSfxTouchHandler = self.sliderSfxTouch.Entity:ConnectEvent(UITouchEndDragEvent, self.OnSfxReleased)
end
if self.btnRevertKeys ~= nil then
	self.btnRevertKeysHandler = self.btnRevertKeys.Entity:ConnectEvent(ButtonClickEvent, self.OnRevertKeysClick)
end
if self.btnSyncAdjust ~= nil then
	self.btnSyncAdjustHandler = self.btnSyncAdjust.Entity:ConnectEvent(ButtonClickEvent, self.OnSyncAdjustClick)
end
if self.btnApply ~= nil then
	self.btnApplyHandler = self.btnApply.Entity:ConnectEvent(ButtonClickEvent, self.OnApplyClick)
end
if self.btnClose ~= nil then
	self.btnCloseHandler = self.btnClose.Entity:ConnectEvent(ButtonClickEvent, self.OnCloseClick)
end

self.keyDownHandler = _InputService:ConnectEvent(KeyDownEvent, self.OnGlobalKeyDown)
self.settingsHandler = _SettingsManager:ConnectEvent(SettingsChangedEvent, self.OnSettingsChanged)

self:HideNotice()
log("[SettingsLogic] OnBeginPlay wired")
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="OnEndPlay" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments />
      <Code><![CDATA[
for _, slot in ipairs(self._T.slots) do
	if slot.clickHandler then slot.btn.Entity:DisconnectEvent(ButtonClickEvent, slot.clickHandler) end
end
if self.sliderBgmHandler then self.sliderBgm.Entity:DisconnectEvent(SliderValueChangedEvent, self.sliderBgmHandler) end
if self.sliderSfxHandler then self.sliderSfx.Entity:DisconnectEvent(SliderValueChangedEvent, self.sliderSfxHandler) end
if self.sliderSfxTouchHandler then self.sliderSfxTouch.Entity:DisconnectEvent(UITouchEndDragEvent, self.sliderSfxTouchHandler) end
if self.btnRevertKeysHandler then self.btnRevertKeys.Entity:DisconnectEvent(ButtonClickEvent, self.btnRevertKeysHandler) end
if self.btnSyncAdjustHandler then self.btnSyncAdjust.Entity:DisconnectEvent(ButtonClickEvent, self.btnSyncAdjustHandler) end
if self.btnApplyHandler then self.btnApply.Entity:DisconnectEvent(ButtonClickEvent, self.btnApplyHandler) end
if self.btnCloseHandler then self.btnClose.Entity:DisconnectEvent(ButtonClickEvent, self.btnCloseHandler) end
if self.openBtnHandler then self.openBtn.Entity:DisconnectEvent(ButtonClickEvent, self.openBtnHandler) end
if self.keyDownHandler then _InputService:DisconnectEvent(KeyDownEvent, self.keyDownHandler) end
if self.settingsHandler then _SettingsManager:DisconnectEvent(SettingsChangedEvent, self.settingsHandler) end
if self.noticeTimerId ~= 0 then _TimerService:ClearTimer(self.noticeTimerId) end
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="BuildDisplayNames" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments />
      <Code><![CDATA[
-- SettingsManager는 "LeftArrow", "Alpha1", "Return" 같은 내부 이름을 준다.
-- 화면에는 화살표와 짧은 표기로 보여준다.
self._T.displayByName = {
	LeftArrow = "←", RightArrow = "→", UpArrow = "↑", DownArrow = "↓",
	Return = "Enter", Escape = "Esc", Space = "Space", Tab = "Tab",
	LeftShift = "Shift", RightShift = "R-Shift",
	LeftControl = "Ctrl", RightControl = "R-Ctrl",
	LeftAlt = "Alt", RightAlt = "R-Alt",
}
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="DisplayOf" ExecSpace="ClientOnly">
      <Return Type="string" SyncDirection="None" />
      <Arguments>
        <CodeBlockProperty Name="keyName" Type="string" SyncDirection="None" />
      </Arguments>
      <Code><![CDATA[
-- [변경] 배정된 키가 없어 칸이 비는 경우에만 waitingLabel("입력 대기")을 띄운다.
if keyName == nil or keyName == "" then return self.waitingLabel end
local mapped = self._T.displayByName[keyName]
if mapped ~= nil then return mapped end
-- "Alpha7" → "7"
local digit = string.match(keyName, "^Alpha(%d)$")
if digit ~= nil then return digit end
return keyName
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="WaitingTextOf" ExecSpace="ClientOnly">
      <Return Type="string" SyncDirection="None" />
      <Arguments>
        <CodeBlockProperty Name="keyName" Type="string" SyncDirection="None" />
      </Arguments>
      <Code><![CDATA[
-- [추가] 입력을 받는 중인 행의 표시. 키 이름을 대괄호로 감싼다.
return "[" .. self:DisplayOf(keyName) .. "]"
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="SlotByAction" ExecSpace="ClientOnly">
      <Return Type="table" SyncDirection="None" />
      <Arguments>
        <CodeBlockProperty Name="action" Type="string" SyncDirection="None" />
      </Arguments>
      <Code><![CDATA[
for _, slot in ipairs(self._T.slots) do
	if slot.action == action then return slot end
end
return nil
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="OnOpenBtnClick" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments />
      <Code><![CDATA[
-- 로비의 설정 버튼. 한 번 더 누르면 닫힌다.
self:Toggle()
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="Toggle" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments />
      <Code><![CDATA[
if self.isOpen then
	self:OnCloseClick()
else
	self:OpenSettings()
end
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="OpenSettings" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments />
      <Code><![CDATA[
if self.isOpen then return end
if isvalid(self.settingsGroup) == false then
	log("[SettingsLogic] settingsGroup 미연결 — 열 수 없다")
	return
end
self.isOpen = true
self.waitingAction = ""
self.settingsGroup.Enable = true
if isvalid(self.hideWhileOpen) then
	self.hideWhileOpen.Enable = false
end
self:RefreshFromDraft()
-- PlayerInputBridge가 이 알림을 받아 캐릭터 입력을 막는다.
_SettingsManager:NotifyUIOpened()
log("[SettingsLogic] Opened")
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="CloseImmediate" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments />
      <Code><![CDATA[
if isvalid(self.settingsGroup) then
	self.settingsGroup.Enable = false
end
if isvalid(self.hideWhileOpen) then
	self.hideWhileOpen.Enable = true
end
self.isOpen = false
self.waitingAction = ""
self:HideNotice()
_SettingsManager:NotifyUIClosed()
log("[SettingsLogic] Closed")
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="RefreshFromDraft" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments />
      <Code><![CDATA[
local draft = _SettingsManager:GetDraft()
if self.sliderBgm ~= nil then self.sliderBgm.Value = draft.volume.bgm end
if self.valueBgm ~= nil then self.valueBgm.Text = tostring(draft.volume.bgm) end
if self.sliderSfx ~= nil then self.sliderSfx.Value = draft.volume.sfx end
if self.valueSfx ~= nil then self.valueSfx.Text = tostring(draft.volume.sfx) end

for _, slot in ipairs(self._T.slots) do
	if slot.text ~= nil then
		slot.text.Text = self:DisplayOf(draft.keys[slot.action])
	end
	if isvalid(slot.icon) then slot.icon.Enable = false end
end

if self.syncValueText ~= nil then
	self.syncValueText.Text = self:FormatSync(self:GetSyncOffsetMs())
end
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="OnBgmChanged" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments>
        <CodeBlockProperty Name="event" Type="SliderValueChangedEvent" SyncDirection="None" />
      </Arguments>
      <Code><![CDATA[
-- 값 반영은 SettingsManager가, 실제 볼륨은 _SoundChannels가 이벤트를 받아 처리한다.
_SettingsManager:SetDraftVolume("bgm", math.floor(event.Value + 0.5))
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="OnSfxChanged" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments>
        <CodeBlockProperty Name="event" Type="SliderValueChangedEvent" SyncDirection="None" />
      </Arguments>
      <Code><![CDATA[
_SettingsManager:SetDraftVolume("sfx", math.floor(event.Value + 0.5))
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="OnSfxReleased" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments>
        <CodeBlockProperty Name="event" Type="UITouchEndDragEvent" SyncDirection="None" />
      </Arguments>
      <Code><![CDATA[
-- 값 변경 이벤트에 미리듣기를 걸면 드래그 중 초당 수십 번 재생되므로
-- 손을 놓는 순간에만 1회 재생한다.
_SoundChannels:PreviewSfx()
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="OnKeyButtonClick" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments>
        <CodeBlockProperty Name="slot" Type="table" SyncDirection="None" />
      </Arguments>
      <Code><![CDATA[
-- ==============================================================
-- [변경] confirmWithButton = false (기본, 한 번 누르기)
--   [변경] 클릭 → 키 입력을 받기 시작. 칸의 글자는 건드리지 않는다.
--   키 입력 → 바로 반영 + 저장.
--   대기 중에 [변경]을 다시 누르면 취소.
--
-- confirmWithButton = true (두 번 누르기)
--   [변경] 클릭 → 대기 시작 → 키 입력(칸에 미리 표시, 저장 안 함)
--   → [변경] 다시 클릭 → 그때 반영 + 저장.
-- ==============================================================
if self.waitingAction == slot.action then
	if self._T.confirmWithButton == false then
		-- 한 번 누르기 모드: 같은 버튼을 다시 누르면 대기 취소
		self:CancelWaiting()
		self:ShowNotice("키 변경을 취소했습니다")
		return
	end
	if self._T.pendingKeyName == "" then
		self:ShowNotice("먼저 변경할 키를 누르세요")
		return
	end
	self:CommitPendingKey()
	return
end

-- [변경] 다른 행이 대기 중이었으면 그 행을 먼저 마무리하고 넘어온다.
-- 예전에는 CancelWaiting으로 버려서, 1번 행에 키를 넣어 둔 채 2번 행의
-- [변경]을 누르면 1번 행이 예전 값으로 되돌아갔다.
if self.waitingAction ~= "" then
	self:EndWaiting(true)
end

self.waitingAction = slot.action
self._T.pendingKeyName = ""
-- [추가] 어느 행이 입력을 받는 중인지 보이게 한다. 키 이름은 지우지 않고
-- 대괄호로 감싸기만 하므로 현재 키도 계속 보인다.
if slot.text ~= nil then
	slot.text.Text = self:WaitingTextOf(_SettingsManager:GetKeyName(slot.action))
end
if isvalid(slot.icon) then
	slot.icon.Enable = true
end
if self._T.confirmWithButton then
	self:ShowNotice("변경할 키를 누른 뒤 [변경]을 다시 누르세요. (ESC: 취소)")
else
	self:ShowNotice("변경할 키를 누르세요. (ESC: 취소)")
end
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="CommitPendingKey" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments />
      <Code><![CDATA[
-- [추가] 미리 받아 둔 키를 실제로 반영하고 저장한다.
local action = self.waitingAction
local newName = self._T.pendingKeyName
if action == "" then return end
if newName == "" then return end

local slot = self:SlotByAction(action)
if slot ~= nil and isvalid(slot.icon) then
	slot.icon.Enable = false
end
self.waitingAction = ""
self._T.pendingKeyName = ""

-- SetDraftKey가 "keys" 이벤트를 발행하고, OnSettingsChanged가 표시를 갱신한다.
_SettingsManager:SetDraftKey(action, newName)

-- 저장. 결과는 "applied" / "applyFailed" 이벤트로 돌아온다(OnSettingsChanged).
if _SettingsManager:HasUnsavedChanges() then
	_SettingsManager:Apply()
else
	self:ShowNotice("변경되었습니다")
end
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="EndWaiting" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments>
        <CodeBlockProperty Name="keepChange" Type="boolean" SyncDirection="None" />
      </Arguments>
      <Code><![CDATA[
-- [추가] 대기 상태를 끝낸다.
--   keepChange = true  : 받아 둔 키가 있으면 저장하고 끝낸다(값을 버리지 않는다)
--   keepChange = false : 원래 키로 되돌린다
-- ESC로 명시적으로 취소할 때만 false를 쓴다. 다른 행으로 넘어가거나
-- 창을 닫는 것은 "취소"가 아니므로 값을 지키는 쪽이 맞다.
if self.waitingAction == "" then return end
if keepChange and self._T.pendingKeyName ~= "" then
	self:CommitPendingKey()
	return
end
self:CancelWaiting()
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="CancelWaiting" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments />
      <Code><![CDATA[
-- ESC / 다른 행으로 이동 / 창 닫기: 미리 표시된 키를 버리고 원래 키로 되돌린다.
if self.waitingAction == "" then return end
local slot = self:SlotByAction(self.waitingAction)
self.waitingAction = ""
self._T.pendingKeyName = ""
if slot == nil then return end
if slot.text ~= nil then
	slot.text.Text = self:DisplayOf(_SettingsManager:GetDraft().keys[slot.action])
end
if isvalid(slot.icon) then
	slot.icon.Enable = false
end
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="OnGlobalKeyDown" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments>
        <CodeBlockProperty Name="event" Type="KeyDownEvent" SyncDirection="None" />
      </Arguments>
      <Code><![CDATA[
if self.waitingAction ~= "" then
	self:HandleRebindKeyDown(event)
	return
end
if self.isOpen and _SettingsManager:KeyNameFromCode(event.key) == "Escape" then
	self:OnCloseClick()
end
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="FindVisibleActionUsingKey" ExecSpace="ClientOnly">
      <Return Type="string" SyncDirection="None" />
      <Arguments>
        <CodeBlockProperty Name="keyName" Type="string" SyncDirection="None" />
        <CodeBlockProperty Name="excludingAction" Type="string" SyncDirection="None" />
      </Arguments>
      <Code><![CDATA[
-- [추가] 이 화면에 실제로 보이는 3행(1번 입력/2번 입력/확인) 중에서만
-- 같은 키를 쓰는 액션을 찾는다. 없으면 "".
if self._T == nil or self._T.slots == nil then return "" end
for _, slot in ipairs(self._T.slots) do
	local a = slot.action
	if a ~= nil and a ~= "" and a ~= excludingAction then
		if _SettingsManager:GetKeyName(a) == keyName then
			return a
		end
	end
end
return ""
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="HandleRebindKeyDown" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments>
        <CodeBlockProperty Name="event" Type="KeyDownEvent" SyncDirection="None" />
      </Arguments>
      <Code><![CDATA[
local action = self.waitingAction
local newName = _SettingsManager:KeyNameFromCode(event.key)

if newName == "Escape" then
	self:CancelWaiting()
	self:ShowNotice("키 변경을 취소했습니다")
	return
end

-- [변경] 거절돼도 대기 상태는 유지한다. 다른 키를 바로 다시 누를 수 있게.
if _SettingsManager:IsKeyAllowed(newName) == false then
	self:ShowNotice("이 키는 사용할 수 없습니다")
	return
end

-- [변경] 중복 검사는 이 화면에 보이는 3행끼리만 한다.
-- SettingsManager:FindActionUsingKey는 8개 액션을 전부 훑는데,
-- MoveLeft/MoveRight 등 캐릭터 조작 키는 이 화면에 없어서 플레이어가
-- 비울 수가 없다. 그래서 A(=MoveLeft 기본값)나 D(=MoveRight 기본값)를
-- 넣으려 하면 영영 거절당한다.
if self:FindVisibleActionUsingKey(newName, action) ~= "" then
	self:ShowNotice("이미 사용 중인 키입니다")
	return
end

self._T.pendingKeyName = newName

if self._T.confirmWithButton == false then
	-- 한 번 누르기: 누른 그 자리에서 반영 + 저장
	self:CommitPendingKey()
	return
end

-- 두 번 누르기: 칸에 미리 보여 주기만 하고, 저장은 [변경]을 다시 눌렀을 때.
local slot = self:SlotByAction(action)
if slot ~= nil and slot.text ~= nil then
	slot.text.Text = self:WaitingTextOf(newName)
end
self:ShowNotice("[변경]을 다시 누르면 저장됩니다")
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="OnRevertKeysClick" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments />
      <Code><![CDATA[
-- "되돌리기"는 키 설정 섹션의 버튼이므로 키만 기본값으로 돌린다.
-- SettingsManager에는 키 전용 초기화가 없어, 볼륨을 먼저 기억해 두고
-- 전체 초기화 후 볼륨만 되돌려 놓는다(SettingsManager 수정 없이 처리).
self:CancelWaiting()

local draft = _SettingsManager:GetDraft()
local bgm = draft.volume.bgm
local sfx = draft.volume.sfx
local voice = draft.volume.voice

_SettingsManager:RestoreDraftDefaults()
_SettingsManager:SetDraftVolume("bgm", bgm)
_SettingsManager:SetDraftVolume("sfx", sfx)
_SettingsManager:SetDraftVolume("voice", voice)

self:RefreshFromDraft()

-- [변경] 되돌린 값을 저장까지 한다. 예전에는 draft만 바꿔서, 되돌리기를
-- 누른 뒤 창을 닫으면 OnCloseClick의 RevertDraftToSaved가 예전 키를 도로
-- 불러왔다(= 되돌리기가 먹히지 않았다). 키 변경이 즉시 저장되는 지금
-- 방식과도 맞춘다.
if _SettingsManager:HasUnsavedChanges() then
	_SettingsManager:Apply()
end
self:ShowNotice("키 설정을 기본값으로 되돌렸습니다")
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="GetSyncOffsetMs" ExecSpace="ClientOnly">
      <Return Type="integer" SyncDirection="None" />
      <Arguments />
      <Code><![CDATA[
-- SettingsManager가 syncOffsetMs를 지원하면 그 값을, 아니면 로컬 값을 쓴다.
local draft = _SettingsManager:GetDraft()
if draft.syncOffsetMs ~= nil then
	return draft.syncOffsetMs
end
return self.localSyncOffsetMs
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="SetSyncOffsetMs" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments>
        <CodeBlockProperty Name="ms" Type="integer" SyncDirection="None" />
      </Arguments>
      <Code><![CDATA[
local clamped = math.max(self.syncMinMs, math.min(self.syncMaxMs, ms))
self.localSyncOffsetMs = clamped
-- SettingsManager가 이 항목을 지원하게 되면 저장까지 함께 따라간다.
local draft = _SettingsManager:GetDraft()
if draft.syncOffsetMs ~= nil then
	draft.syncOffsetMs = clamped
end
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="FormatSync" ExecSpace="ClientOnly">
      <Return Type="string" SyncDirection="None" />
      <Arguments>
        <CodeBlockProperty Name="ms" Type="integer" SyncDirection="None" />
      </Arguments>
      <Code><![CDATA[
if ms > 0 then
	return "+" .. tostring(ms) .. "ms"
end
return tostring(ms) .. "ms"
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="OnSyncAdjustClick" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments />
      <Code><![CDATA[
-- TODO: 원래 "조절하기"는 비트에 맞춰 입력을 받아 오프셋을 계산하는
-- 보정 화면을 여는 버튼이다. 그 화면이 아직 없어서, 값을 직접 확인할 수
-- 있도록 클릭마다 syncStepMs 만큼 올리고 상한을 넘으면 하한으로 돌아가는
-- 임시 동작을 넣어 두었다.
local nextValue = self:GetSyncOffsetMs() + self.syncStepMs
if nextValue > self.syncMaxMs then
	nextValue = self.syncMinMs
end
self:SetSyncOffsetMs(nextValue)
if self.syncValueText ~= nil then
	self.syncValueText.Text = self:FormatSync(self:GetSyncOffsetMs())
end
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="OnApplyClick" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments />
      <Code><![CDATA[
self:EndWaiting(true)
if _SettingsManager:HasUnsavedChanges() == false then
	self:ShowNotice("변경된 내용이 없습니다")
	return
end
-- 결과는 "applied" / "applyFailed" 이벤트로 돌아온다(OnSettingsChanged).
_SettingsManager:Apply()
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="OnCloseClick" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments />
      <Code><![CDATA[
-- [변경] 닫을 때 되돌리지 않고 저장한다.
-- 예전에는 RevertDraftToSaved로 되돌렸는데, 저장 왕복(Apply → 서버 →
-- ReceiveSaveResult)이 끝나기 전에 창을 닫으면 saved가 아직 옛 값이라
-- 방금 바꾼 키가 통째로 날아갔다. 키는 [변경]에서 이미 저장되므로,
-- 여기서는 아직 저장 안 된 것(볼륨 등)만 마저 저장하고 닫는다.
self:EndWaiting(true)
if _SettingsManager:HasUnsavedChanges() then
	_SettingsManager:Apply()
end
self.localSyncOffsetMs = self:GetSyncOffsetMs()
self:CloseImmediate()
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="OnSettingsChanged" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments>
        <CodeBlockProperty Name="event" Type="SettingsChangedEvent" SyncDirection="None" />
      </Arguments>
      <Code><![CDATA[
if event.kind == "volume" then
	local draft = _SettingsManager:GetDraft()
	if event.name == "bgm" and self.valueBgm ~= nil then
		self.valueBgm.Text = tostring(draft.volume.bgm)
	elseif event.name == "sfx" and self.valueSfx ~= nil then
		self.valueSfx.Text = tostring(draft.volume.sfx)
	end
	-- voice는 이 화면에 없으므로 무시(저장값은 그대로 유지된다)

elseif event.kind == "keys" then
	local slot = self:SlotByAction(event.name)
	if slot ~= nil and slot.text ~= nil then
		slot.text.Text = self:DisplayOf(_SettingsManager:GetDraft().keys[slot.action])
	end

elseif event.kind == "loaded" or event.kind == "restored" or event.kind == "reverted" then
	self:RefreshFromDraft()

elseif event.kind == "applied" then
	-- 이 화면은 "적용하기"와 "닫기"가 따로 있으므로 저장 후에도 닫지 않는다.
	self:RefreshFromDraft()
	self:ShowNotice("설정을 저장했습니다")

elseif event.kind == "applyFailed" then
	self:ShowNotice("저장에 실패했습니다. 다시 시도해 주세요")
end
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="ShowNotice" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments>
        <CodeBlockProperty Name="message" Type="string" SyncDirection="None" />
      </Arguments>
      <Code><![CDATA[
-- 안내 문구 슬롯은 선택이다. 연결하지 않았으면 로그만 남긴다.
if isvalid(self.noticeLabel) == false then
	log("[SettingsLogic] " .. message)
	return
end
if self.noticeText ~= nil then
	self.noticeText.Text = message
end
self.noticeLabel.Enable = true

if self.noticeTimerId ~= 0 then
	_TimerService:ClearTimer(self.noticeTimerId)
	self.noticeTimerId = 0
end
self.noticeTimerId = _TimerService:SetTimerOnce(function()
	self.noticeTimerId = 0
	self:HideNotice()
end, self.noticeDurationSeconds)
]]></Code>
    </CodeBlockMethod>
    <CodeBlockMethod Name="HideNotice" ExecSpace="ClientOnly">
      <Return Type="void" SyncDirection="None" />
      <Arguments />
      <Code><![CDATA[
if isvalid(self.noticeLabel) then
	self.noticeLabel.Enable = false
end
]]></Code>
    </CodeBlockMethod>
  </Methods>
  <EntityEventHandlers />
</CodeBlockFile>